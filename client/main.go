package main

import (
	"fmt"
)

// TODO:
// * Command/arg parsing via cobra or similar
// * Top-level -v / --verbose flag that sets the log level via
//   log.SetGlobalLevel

func main() {
	fmt.Println("TODO")
}
